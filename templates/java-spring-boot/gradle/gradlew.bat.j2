@rem Gradle wrapper stub — el JAR real se descarga automaticamente.
@rem Este archivo se genera como placeholder; reemplazar con el wrapper real
@rem ejecutando: gradle wrapper --gradle-version 9.4.1
@echo ERROR: Gradle wrapper JAR no encontrado.
@echo Ejecute 'gradle wrapper --gradle-version 9.4.1' para generar el wrapper completo.
@exit /b 1
