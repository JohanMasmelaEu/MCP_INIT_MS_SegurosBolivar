package {{ base_package }}.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuracion de Spring WS + WS-Security para integraciones SOAP legacy.
 * TODO: Configurar interceptores WS-Security segun el servicio SOAP a consumir.
 */
@Configuration
public class WsSecurityConfiguration {
    // Agregar beans de WebServiceTemplate y WS-Security interceptors aqui
}
