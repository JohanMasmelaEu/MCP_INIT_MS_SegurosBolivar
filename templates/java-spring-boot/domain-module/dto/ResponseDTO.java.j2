package {{ base_package }}.{{ module.module_name }}.dto;

import lombok.Getter;
import lombok.Setter;
{% for imp in field_imports %}
import {{ imp }};
{% endfor %}

import java.time.LocalDateTime;

/**
 * DTO de respuesta para {{ module.entity_name }}.
 * Incluye campos de auditoria.
 */
@Getter
@Setter
public class {{ module.entity_name }}ResponseDTO {

    private Long id{{ module.entity_name }};
{% if module.has_cod_field %}
    private Long cod{{ module.entity_name }};
{% endif %}
{% for field in module.fields %}
    private {{ field.field_type.value }} {{ field.name }};
{% endfor %}
    private Boolean estado;
    private LocalDateTime fechaCreacion;
    private String usuarioCreacion;
    private LocalDateTime fechaActualizacion;
    private String usuarioActualizacion;
}
