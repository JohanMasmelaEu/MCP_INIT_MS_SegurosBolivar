package {{ base_package }}.{{ module.module_name }}.controller;

import {{ base_package }}.commons.ApiResponseData;
import {{ base_package }}.commons.ApiResponseDTO;
import {{ base_package }}.{{ module.module_name }}.dto.{{ module.entity_name }}ActualizarRequestDTO;
import {{ base_package }}.{{ module.module_name }}.dto.{{ module.entity_name }}CrearRequestDTO;
import {{ base_package }}.{{ module.module_name }}.dto.{{ module.entity_name }}FiltrosRequestDTO;
import {{ base_package }}.{{ module.module_name }}.dto.{{ module.entity_name }}ResponseDTO;
import {{ base_package }}.{{ module.module_name }}.services.{{ module.entity_name }}Service;
import {{ base_package }}.errorhandling.BolivarBusinessException;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;
import org.springframework.data.web.PageableDefault;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.Operation;

/**
 * CRUD de la entidad {{ module.entity_name }}.
 */
@Tag(name = "{{ module.entity_name }}", description = "CRUD de {{ module.entity_name }}: crear, listar con filtros paginados, obtener por ID, actualizar y eliminar.")
@RestController
@RequestMapping("/api/v1/{{ module.resource_path_resolved }}")
@RequiredArgsConstructor
public class {{ module.entity_name }}Controller {

    private final {{ module.entity_name }}Service {{ module.entity_name | lower }}Service;

    @Operation(summary = "Crear {{ module.entity_name | lower }}", description = "Crea un nuevo registro de {{ module.entity_name }}. Respuesta 201.")
    @PostMapping(consumes = MediaType.APPLICATION_JSON_VALUE, produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ApiResponseDTO<{{ module.entity_name }}ResponseDTO>> crear(
            @Valid @RequestBody {{ module.entity_name }}CrearRequestDTO request) throws BolivarBusinessException {
        {{ module.entity_name }}ResponseDTO response = {{ module.entity_name | lower }}Service.crear(request);
        return ResponseEntity.status(HttpStatus.CREATED).body(ApiResponseData.ofCreated(response));
    }

    @Operation(summary = "Obtener {{ module.entity_name | lower }} por ID", description = "Consulta por identificador. Respuesta 200 o 404.")
    @GetMapping(value = "/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ApiResponseDTO<{{ module.entity_name }}ResponseDTO>> obtenerPorId(
            @PathVariable Long id) throws BolivarBusinessException {
        return ResponseEntity.ok(ApiResponseData.of({{ module.entity_name | lower }}Service.obtenerPorId(id)));
    }

    @Operation(summary = "Listar {{ module.entity_name | lower }}", description = "Listado paginado con filtros opcionales. Default: page=0, size=10.")
    @GetMapping(produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ApiResponseDTO<Page<{{ module.entity_name }}ResponseDTO>>> listar(
            @PageableDefault(size = 10, sort = "id{{ module.entity_name }}", direction = Sort.Direction.ASC) Pageable pageable,
            @ModelAttribute {{ module.entity_name }}FiltrosRequestDTO filtros) throws BolivarBusinessException {
        return ResponseEntity.ok(ApiResponseData.of({{ module.entity_name | lower }}Service.listar(pageable, filtros)));
    }

    @Operation(summary = "Actualizar {{ module.entity_name | lower }}", description = "Actualiza un registro existente. Respuesta 200 o 404.")
    @PutMapping(value = "/{id}", consumes = MediaType.APPLICATION_JSON_VALUE, produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ApiResponseDTO<{{ module.entity_name }}ResponseDTO>> actualizar(
            @PathVariable Long id,
            @Valid @RequestBody {{ module.entity_name }}ActualizarRequestDTO request) throws BolivarBusinessException {
        return ResponseEntity.ok(ApiResponseData.of({{ module.entity_name | lower }}Service.actualizar(id, request)));
    }

    @Operation(summary = "Inactivar {{ module.entity_name | lower }}", description = "Inactiva logicamente. Respuesta 204 o 404.")
    @DeleteMapping(value = "/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Void> eliminar(@PathVariable Long id) throws BolivarBusinessException {
        {{ module.entity_name | lower }}Service.eliminar(id);
        return ResponseEntity.noContent().build();
    }
}
