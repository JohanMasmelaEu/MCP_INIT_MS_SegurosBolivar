# Changelog — develop

## [No publicado]

### Agregado
- Se inicializo el proyecto {{ project_name }} con arquetipo MCP_INIT_MS_SegurosBolivar
- Se crearon modulos de dominio: {{ modules | map(attribute='entity_name') | join(', ') }}
- Se configuro Docker Compose local (LocalStack + Mock OAuth{% if observability.datadog_enabled %} + Datadog Agent{% endif %})
- Se configuro CI/CD con GitHub Actions (template institucional)
- Se configuro SpringDoc OpenAPI + Swagger UI
- Se configuro JaCoCo con cobertura minima 85%
